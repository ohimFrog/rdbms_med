# 데이터베이스 설계 - 발표 대본 (한국어)

## 도입부

> "이제 저희 데이터베이스 설계에 대해 설명드리겠습니다. 저희는 데이터 무결성을 보장하고 중복을 최소화하기 위해 **제3정규형(3NF)**을 따라 데이터베이스를 설계했습니다."

---

## 1. 데이터 수집

> "먼저 데이터 출처에 대해 말씀드리겠습니다. 저희는 공개 데이터셋에서 한국 의약품 데이터를 수집했습니다. 원본 데이터는 CSV 형식으로, 약 **29,000개의 의약품 레코드**를 포함하고 있으며 크기는 약 **10메가바이트**입니다.
>
> 원본 CSV에는 8개의 컬럼이 있었습니다: 제품명, 제조사명, 효능, 용법용량, 주의사항, 약물상호작용, 부작용, 보관방법. 모든 정보는 한국어로 되어 있었습니다."

---

## 2. 왜 정규화가 필요한가?

> "처음에는 이 모든 데이터가 하나의 평면 테이블에 있었습니다. 이로 인해 여러 문제가 발생했습니다:
>
> **첫째**, 제조사 이름이 모든 약품마다 반복되었습니다. 한 회사가 100개의 약을 생산하면, 회사 이름이 100번 나타납니다. 이는 저장 공간을 낭비합니다.
>
> **둘째**, 제조사 이름을 업데이트해야 할 경우, 수천 개의 행을 업데이트해야 합니다. 이를 **갱신 이상(Update Anomaly)**이라고 합니다.
>
> **셋째**, 관련되어 있지만 별개의 개념을 하나의 테이블에 저장하는 것은 **단일 책임 원칙**을 위반합니다.
>
> 이러한 문제를 해결하기 위해 **제3정규형(3NF) 정규화**를 적용했습니다."

---

## 3. 테이블별 설명

### 테이블 1: manufacturer (제조사)

> "첫 번째 테이블은 **manufacturer**입니다. 이 테이블은 제약회사 정보를 저장합니다.
>
> 두 개의 컬럼이 있습니다:
>
> - `id`: 기본키(Primary Key), 자동 증가 정수
> - `name`: 회사명, 고유(UNIQUE)해야 함
>
> 제조사를 별도의 테이블로 분리함으로써, 해당 회사가 얼마나 많은 약을 생산하든 상관없이 각 회사 이름을 **한 번만** 저장합니다."

### 테이블 2: drug_basic (약품 기본정보)

> "두 번째 테이블은 **drug_basic**입니다. 이것이 약품 기본 정보를 저장하는 중심 테이블입니다.
>
> 네 개의 컬럼이 있습니다:
>
> - `id`: 기본키
> - `name`: 약품 제품명
> - `manufacturer_id`: manufacturer 테이블을 참조하는 **외래키(Foreign Key)**
> - `storage`: 보관방법
>
> 제조사 이름을 직접 저장하는 대신, manufacturer 테이블에 대한 참조를 저장한다는 점에 주목하세요. 이것은 **1:N(일대다) 관계**입니다: 한 제조사가 여러 약품을 가질 수 있습니다."

### 테이블 3: drug_usage (약품 효능/용법)

> "세 번째 테이블은 **drug_usage**입니다. 치료 관련 정보를 저장합니다.
>
> - `id`: 기본키
> - `drug_id`: drug_basic에 대한 외래키, **UNIQUE**로 표시됨
> - `effect`: 약의 효능
> - `dosage`: 복용 방법
>
> drug_id에 대한 **UNIQUE 제약조건**은 이것이 **1:1(일대일) 관계**임을 보장합니다. 각 약품은 정확히 하나의 usage 레코드를 가집니다."

### 테이블 4: drug_warning (약품 주의사항)

> "네 번째 테이블은 **drug_warning**입니다. 안전 관련 정보를 포함합니다.
>
> - `id`: 기본키
> - `drug_id`: 외래키, UNIQUE
> - `precaution`: 일반적인 경고 및 주의사항
> - `interaction`: 다른 약물과의 상호작용
>
> 이것도 마찬가지로 drug_basic과 **1:1 관계**입니다."

### 테이블 5: drug_side_effect (약품 부작용)

> "다섯 번째 테이블은 **drug_side_effect**입니다.
>
> - `id`: 기본키
> - `drug_id`: 외래키, UNIQUE
> - `side_effect`: 발생 가능한 이상반응
>
> 이렇게 분리함으로써 필요할 때 부작용 정보만 독립적으로 조회할 수 있습니다."

### 테이블 6: search_history (검색 기록)

> "여섯 번째이자 마지막 테이블은 **search_history**입니다. 사용자 검색을 기록합니다.
>
> - `id`: 기본키
> - `drug_id`: drug_basic에 대한 외래키
> - `searched_text`: 사용자가 실제로 검색한 텍스트
> - `searched_at`: 검색이 발생한 시간(타임스탬프)
>
> 여기서 drug_id는 **UNIQUE가 아닙니다**. 이것은 **1:N(일대다) 관계**입니다: 하나의 약품이 여러 사용자에 의해 여러 번 검색될 수 있습니다."

---

## 4. 관계 요약

> "저희 데이터베이스의 관계를 요약해 드리겠습니다:
>
> - **Manufacturer → Drug**: 일대다(1:N). 한 제조사가 여러 약품을 생산합니다.
> - **Drug → Usage/Warning/SideEffect**: 일대일(1:1). 각 약품은 정확히 하나씩 가집니다.
> - **Drug → SearchHistory**: 일대다(1:N). 하나의 약품이 여러 검색 레코드를 가질 수 있습니다.
>
> 이러한 관계는 **외래키 제약조건**을 통해 강제되어 참조 무결성을 보장합니다."

---

## 5. 3NF 검증

> "저희 설계가 제3정규형을 만족하는지 검증해 보겠습니다:
>
> **제1정규형(1NF)**: 모든 컬럼이 원자값을 포함합니다. 반복 그룹이 없습니다.
>
> **제2정규형(2NF)**: 모든 비키 속성이 기본키에 완전히 종속됩니다. 부분 종속성이 없습니다.
>
> **제3정규형(3NF)**: 이행적 종속성이 없습니다. 예를 들어, 제조사 이름이 더 이상 manufacturer_id를 통해 drug_id에 간접적으로 종속되지 않습니다.
>
> 3NF를 달성함으로써 데이터 중복을 제거하고 데이터 일관성을 보장했습니다."

---

## 6. 이 설계의 장점

> "이 정규화된 설계는 여러 이점을 제공합니다:
>
> **첫째**, **쿼리 유연성**. JOIN 쿼리를 쉽게 작성하여 관련 데이터를 가져올 수 있습니다. 예를 들어, 특정 제조사의 모든 약품을 가져올 수 있습니다.
>
> **둘째**, **집계 기능**. GROUP BY를 사용하여 각 제조사가 생산하는 약품 수와 같은 통계를 얻을 수 있습니다.
>
> **셋째**, **데이터 무결성**. 외래키 제약조건이 고아 레코드를 방지하고 일관성을 유지합니다.
>
> **넷째**, **확장성**. 검색 기록 추적과 같은 새로운 기능을 추가할 때 기존 테이블을 수정하지 않고 새 테이블 하나만 추가하면 됐습니다."

---

## 마무리

> "요약하면, 저희는 평면 CSV 파일을 3NF 원칙에 따라 6개의 테이블로 구성된 잘 구조화된 관계형 데이터베이스로 변환했습니다. 이 설계는 데이터 무결성과 쿼리 효율성을 유지하면서 애플리케이션에 필요한 모든 CRUD 작업을 지원합니다.
>
> 이제 저희 애플리케이션에서 사용하는 SQL 쿼리를 보여드리겠습니다."

---

## 💡 발표 팁

| 구간          | 예상 시간 | 포인트                            |
| ------------- | --------- | --------------------------------- |
| 도입          | 15초      | 3NF 언급으로 전문성 어필          |
| 데이터 수집   | 30초      | 데이터 규모 강조 (29,000건)       |
| 정규화 필요성 | 45초      | Before/After 비교 슬라이드와 함께 |
| 테이블 설명   | 2분       | ER Diagram 가리키며 설명          |
| 관계 요약     | 30초      | 1:1, 1:N 관계 강조                |
| 3NF 검증      | 30초      | 교수님 평가 포인트!               |
| 장점          | 30초      | 실제 쿼리 예시로 연결             |

**총 예상 시간: 약 4~5분**
